// Example: Add any interactivity to your portfolio here
document.querySelectorAll('video').forEach(video => {
    video.addEventListener('click', () => {
        console.log('Video clicked!');
    });
});
